apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  # labels are used as selectors
  selector:
    project: roboshop
    environment: dev
    component: frontend
  ports:
  - protocol: TCP
    port: 80 #service port
    targetPort: 80  #target port nginx